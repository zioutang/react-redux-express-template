import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

const Home = () => {
    return (
        <div>
            <div>

          
            </div>
            <Link to="/login">
                Log In
            </Link>
        </div>
    );
};

export default Home;
